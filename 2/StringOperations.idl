module StringOperations {
    interface StringService {
        string concatenate(in string s1, in string s2);
        string toUpperCase(in string s);
        string toLowerCase(in string s);
    }
}
